import { connect } from 'react-redux';

const mapStateToProps = () => ({});
const mapDispatchToProps = () => ({});

// Importing components from global (Sprockets way)
export default connect(mapStateToProps, mapDispatchToProps)(MapComponent);
